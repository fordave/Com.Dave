<UserControl x:Class="ModelCreator.Views.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelCreator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <DataGrid Height="400" ItemsSource="{Binding GridItems, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
          
            <!--<DataGridComboBoxColumn SelectedValueBinding="{Binding CompanyID}"  DisplayMemberPath="Name" SelectedValuePath="ID">
                <DataGridComboBoxColumn.ElementStyle>
                    <Style TargetType="{x:Type ComboBox}">
                        <Setter Property="ItemsSource" Value="{Binding Path=DataContext.CompanyItems, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
                    </Style>
                </DataGridComboBoxColumn.ElementStyle>               
            </DataGridComboBoxColumn>-->
        </DataGrid>
    </Grid>
</UserControl>
